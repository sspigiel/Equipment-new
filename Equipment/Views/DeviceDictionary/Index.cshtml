@model IEnumerable<Equipment.Models.DeviceDictionary>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceManufacturer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DeviceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceManufacturer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DeviceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeviceDictionaryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeviceDictionaryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeviceDictionaryId })
        </td>
    </tr>
}

</table>
